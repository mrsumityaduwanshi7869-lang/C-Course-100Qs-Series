#include <stdio.h>

int main(){
    int a, b; // declareing two integer variables a and b
    printf("Enter a : "); // show message for user
    scanf("%d", &a);  // take input number from user a value
    printf("Enter b : ");// show message for user
    scanf("%d", &b);  // take input number from user b value
    printf("The value of a is: %d\n", a);  // ouput the value of a
    printf("The value of b is: %d\n", b);  // output the value of b
    return 0;
}