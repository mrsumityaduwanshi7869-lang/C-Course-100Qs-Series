#include <stdio.h>
//Area of Sqquare
int main(){
    int side;
    printf("Enter Side of Square : ");
    scanf("%d",&side);
    printf("Area of Square : %d",side * side); // area = side * side
    return 0;
}